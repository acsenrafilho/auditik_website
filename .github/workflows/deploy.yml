name: Deploy to AWS S3 + CloudFront

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
  CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
  NEXT_PUBLIC_TINA_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_TINA_CLIENT_ID || secrets.PUBLIC_TINA_CLIENT_ID || secrets.public_tina_client_id }}
  NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
  NEXT_PUBLIC_META_PIXEL_ID: ${{ secrets.NEXT_PUBLIC_META_PIXEL_ID }}
  NEXT_PUBLIC_GOOGLE_ADS_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_ADS_ID }}
  NEXT_PUBLIC_GOOGLE_ADS_LABEL_CONTACT: ${{ secrets.NEXT_PUBLIC_GOOGLE_ADS_LABEL_CONTACT }}
  NEXT_PUBLIC_GOOGLE_ADS_LABEL_APPOINTMENT: ${{ secrets.NEXT_PUBLIC_GOOGLE_ADS_LABEL_APPOINTMENT }}
  NEXT_PUBLIC_GOOGLE_ADS_LABEL_WHATSAPP: ${{ secrets.NEXT_PUBLIC_GOOGLE_ADS_LABEL_WHATSAPP }}
  NEXT_PUBLIC_GOOGLE_ADS_LABEL_PHONE: ${{ secrets.NEXT_PUBLIC_GOOGLE_ADS_LABEL_PHONE }}
  TINA_TOKEN: ${{ secrets.TINA_TOKEN || secrets.tina_token }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Validate Tina environment variables
        run: |
          if [ -z "$NEXT_PUBLIC_TINA_CLIENT_ID" ]; then
            echo "Missing NEXT_PUBLIC_TINA_CLIENT_ID/PUBLIC_TINA_CLIENT_ID secret"
            exit 1
          fi
          if [ -z "$TINA_TOKEN" ]; then
            echo "Missing TINA_TOKEN secret"
            exit 1
          fi
          echo "Tina env vars detected for CI build"

      - name: Build Next.js
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync HTML and XML (short cache)
        run: |
          aws s3 sync ./out s3://${{ env.AWS_S3_BUCKET }} \
            --delete \
            --cache-control 'public, max-age=3600' \
            --exclude '*' \
            --include '*.html' \
            --include '*.xml' \
            --include '*.txt' \
            --exclude '.git*'

      - name: Sync static assets (long cache, immutable)
        run: |
          aws s3 sync ./out s3://${{ env.AWS_S3_BUCKET }} \
            --cache-control 'public, max-age=31536000, immutable' \
            --exclude '*.html' \
            --exclude '*.xml' \
            --exclude '*.txt' \
            --exclude '.git*'

      # - name: Invalidate CloudFront cache
      #   run: |
      #     aws cloudfront create-invalidation \
      #       --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
      #       --paths "/*"

      - name: Deployment notification
        if: success()
        run: echo "✅ Successfully deployed to ${{ env.AWS_S3_BUCKET }}"

      - name: Deployment failed notification
        if: failure()
        run: echo "❌ Deployment to AWS S3 failed"
